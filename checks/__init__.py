# checks/__init__.py
# Makes 'checks' a Python package so modules can be imported as:
#   from checks.domain import extract_domain
#   from checks.dns_check import check_spf_record
# No initialization logic needed here — each module is self-contained.
